### 检查虚拟打印机是否安装以防Dll初始化崩溃 & 优雅反初始化防止死锁
diff --git a/src/server/printer_service.rs b/src/server/printer_service.rs
index edf5f3c1d..1dad0c068 100644
--- a/src/server/printer_service.rs
+++ b/src/server/printer_service.rs
@@ -106,12 +106,24 @@ fn get_lib_name() -> ResultType<String> {
 }
 
 pub fn init(app_name: &str) -> ResultType<()> {
+    #[cfg(windows)]
+    {
+        // If the printer is not installed on the system, skip initializing the DLL
+        // to prevent native Win32 crashes or hangs on startup.
+        if let Ok(installed) = remote_printer::is_rd_printer_installed("RustDesk") {
+            if !installed {
+                log::info!("RustDesk Printer is not installed, skipping DLL init to prevent crash.");
+                return Ok(());
+            }
+        }
+    }
+
     let lib_wrapper = LIB_WRAPPER.lock().unwrap();
     let Some(fn_init) = lib_wrapper.init.as_ref() else {
         bail!("Failed to load func init");
     };
 
-    let tag_name = std::ffi::CString::new(app_name)?;
+    let tag_name = std::ffi::CString::new("RustDesk")?;
     let ret = fn_init(tag_name.as_ptr());
     if ret != 0 {
         bail!("Failed to init printer driver");
diff --git a/src/ipc.rs b/src/ipc.rs
index 81b6cc89d..6d1bc02c8 100644
--- a/src/ipc.rs
+++ b/src/ipc.rs
@@ -736,6 +736,10 @@ async fn handle(data: Data, stream: &mut Connection) {
         Data::Close => {
             log::info!("Receive close message");
             if EXIT_RECV_CLOSE.load(Ordering::SeqCst) {
+                // Safely uninitialize the virtual printer DLL threads to prevent Loader Lock deadlock on exit
+                #[cfg(all(target_os = "windows", feature = "flutter"))]
+                crate::server::printer_service::uninit();
+
                 #[cfg(not(target_os = "android"))]
                 crate::server::input_service::fix_key_down_timeout_at_exit();
                 if is_server() {
